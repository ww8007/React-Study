import React, { useState, useRef } from 'react';
import './App.css';

function App() {
  const [input, setInput] = useState({
    id: '',
    pw: '',
  });
  const { id, pw } = input;
  const onChange;
  return <div></div>;
}

export default App;
