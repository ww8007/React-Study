import React, { useState, useRef } from 'react';
import './App.css';

function App() {
  const [input, setInput] = useState({
    id: '',
    pw: '',
  });
  const { id, pw } = input;

  return <div></div>;
}

export default App;
